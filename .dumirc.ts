import { defineConfig } from 'dumi';

export default defineConfig({
  outputPath: 'docs-dist',
  themeConfig: {
    name: 'Dessert UI',
    logo: ''
  },  
  base: '/dessert-ui/',
  publicPath: '/'
  // publicPath: '/dessert-ui/'
});
